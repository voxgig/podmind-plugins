{
  "name": "@voxgig/podw-astro",
  "version": "1.0.2",
  "type": "module",
  "description": "Astro wrapper to use the Podmind widget",
  "exports": {
    ".": "./index.ts"
  },
  "files": [
    "src",
    "index.ts"
  ],
  "keywords": [
    "astro-component",
    "withastro"
  ],
  "scripts": {
    "test": "echo test",
    "clean": "rm -rf dist node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm install && npm run test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push",
    "repo-publish": "npm run clean && npm install --registry https://registry.npmjs.org && npm test && npm run repo-tag && npm publish --registry https://registry.npmjs.org --access=public",
    "repo-publish-quick": "npm run repo-tag && npm publish --registry https://registry.npmjs.org --access=public"
  },
  "devDependencies": {
    "astro": "^4.11.3"
  },
  "peerDependencies": {
    "astro": "^4.0.0"
  },
  "license": "MIT"
}
